<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/top_pick_card"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_margin="10dp"
    android:clickable="true"
    android:foreground="?android:attr/selectableItemBackgroundBorderless"
    app:cardBackgroundColor="@color/light_primary"
    app:cardCornerRadius="10dp"
    app:cardElevation="3dp"
    app:cardUseCompatPadding="true">

    <LinearLayout
        android:layout_width="180dp"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/white">

            <ImageView
                android:id="@+id/top_pick_image"
                style="@style/TopPickListItemImage"/>
        </LinearLayout>

        <LinearLayout
            style="@style/TopPickListItemPadding"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical">

            <TextView
                android:id="@+id/top_pick_item_name"
                style="@style/TopPickListItemName"
                android:minLines="2"
                android:text="Name" />

            <TextView
                android:id="@+id/top_pick_item_price"
                style="@style/TopPickListItemInfo"
                android:text="Price" />

        </LinearLayout>

    </LinearLayout>
</androidx.cardview.widget.CardView>